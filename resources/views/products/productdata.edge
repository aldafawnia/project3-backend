@layout('base')
@section('content')
<h1> User View Product </h1>

<a href="{{route ('admin_addproduct')}}">
  <button type="button" class="btn btn-primary">Add Product</button>
</a>

<table class="table">
        <tr>
          <th></th>
          <th></th>
          <th>ID</th>
          <th>Name</th>
          <th>Price</th>
          <th>Description</th>
          <th>Image</th>
          <th>Stock</th>
          <th>Category</th>
          <th></th>
        </tr>
        @each(p in products)
          <tr>
            <td><a href="{{ route('admin_editproduct', {id:p.id})}}">
            <button type="button" class="btn btn-warning">Edit</button> </a></td>
            <td><a href="{{ route('admin_deleteproduct', {id:p.id})}}">
            <button type="button" class="btn btn-danger">Delete</button> </a></td>
            <td>{{p.id}}</td>
            <td>{{p.product_name}}</td>
            <td>${{p.price/100}}</td>
            <td>{{p.description}}</td>
            <td><img src='{{p.image}}'alt="image" height="140px"/></td>
            <td>{{p.stock}}</td>
            <td>
            @each(c in p.categories)
            {{c.category_name}}
            @endeach
            </td>
            <td></td>
          </tr>
        @endeach
</table>

@endsection
